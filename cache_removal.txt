# Remove SQLite database
rm -f data/database/scanner.db

# Remove DiskCache
rm -rf data/cache/disk

# Remove SQLite cache (if present)
rm -rf data/cache/sqlite

# Remove all Python cache folders
find . -type d -name "__pycache__" -exec rm -rf {} +

# Remove compiled Python files
find . -type f \( -name "*.pyc" -o -name "*.pyo" \) -delete

# Remove pytest cache (if present)
rm -rf .pytest_cache

# Remove Ruff cache (if present)
rm -rf .ruff_cache

# Remove mypy cache (if present)
rm -rf .mypy_cache